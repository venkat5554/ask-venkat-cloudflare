[project]
name = "ask-venkat-cloudflare"
version = "0.1.0"
description = "Ask Venkat portfolio AI assistant on Cloudflare Workers"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]